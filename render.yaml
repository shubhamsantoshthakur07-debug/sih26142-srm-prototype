services:
  - type: web
    name: sih26142-srm-console
    env: python
    buildCommand: pip install -r requirements.txt httpx
    startCommand: uvicorn server.app:app --host 0.0.0.0 --port $PORT
    plan: free
